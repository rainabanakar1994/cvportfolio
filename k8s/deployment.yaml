apiVersion: apps/v1

kind: Deployment

metadata:
  name: myportfolio
  labels:
    app: myportfolio
    environment: production

spec:

  replicas: 2

  selector:
    matchLabels:
      app: myportfolio

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1

  template:
    metadata:
      labels:
        app: myportfolio
    spec:
      containers:
        - name: myportfolio
          image: raina1994/myportfolio:v2
          imagePullPolicy: Always

          ports:
            - containerPort: 8081
              protocol: TCP

          resources:
            requests:
              cpu: "100m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"

